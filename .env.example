# Jev / TypeSafe System One API key, used by the /api/jev decision route.
# Without it the game still runs — the right paddle falls back to a local
# System One reflex — but set this to play against live Jev.
TYPESAFE_API_KEY=""
